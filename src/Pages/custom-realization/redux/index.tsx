import React from 'react';
import Card from 'Components/card';

const CustomRealization = () => {
	return (
		<Card title={'Redux'}>
			<div>Redux</div>
		</Card>
	);
};

export default CustomRealization;
